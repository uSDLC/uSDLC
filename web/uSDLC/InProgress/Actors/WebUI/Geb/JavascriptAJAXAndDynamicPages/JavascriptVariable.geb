browse ThisPage
assert js.aVariable == 1

// same as...

class ExamplePage extends ThisPage {
	static content = {
		aVar { js.aVariable }
	}
}
browse ExamplePage
assert aVar == 1

// We can even access nested variables...

assert js."document.title" == "Javascript, AJAX and Dynamic Pages"
