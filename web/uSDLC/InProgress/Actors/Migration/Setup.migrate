database "jdbc:h2:mem:test:test"
setup = { execute "drop table if exists philosophers" }

verse = { philosopher ->
    row = firstRow "select Verse from philosophers where Philosopher = $philosopher"
    row.Verse
}