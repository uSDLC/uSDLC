if not window.usdlc
	url = window.location
	document.cookie = "currentPage=#{url.pathname}"
	url.href = "#{url.protocol}//#{url.host}?#{url.pathname}"
else
  synopsis = $('div.section:first')
  if synopsis.text().length is 0
    usdlc.form('Synopsis')
    a=1
