package usdlc.reports

import usdlc.Page
import usdlc.db.Database

class Tasks {
	static version = Database.version('usdlc-tasks', 1)

	def processPage(contents) {
//		def page = new Page(contents)
//		page.sections { section ->
//		}
//		page.updated ? page.toString() : contents
	}
}
